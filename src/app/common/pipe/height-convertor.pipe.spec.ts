import { HeightConvertorPipe } from './height-convertor.pipe';

describe('HeightConvertorPipe', () => {
  it('create an instance', () => {
    const pipe = new HeightConvertorPipe();
    expect(pipe).toBeTruthy();
  });
});
